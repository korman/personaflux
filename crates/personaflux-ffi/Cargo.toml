[package]
name = "personaflux-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "C ABI for the PersonaFlux social-affect simulation core"

[lib]
name = "personaflux"
crate-type = ["rlib", "staticlib", "cdylib"]
# The Rust library artifact is used only by the in-workspace ABI integration tests.

[dependencies]
personaflux-core.workspace = true
